
<% @books.each do |book| %>
<div>
  <%= link_to books_path(book.id) do %>
   <% end %>
  <p>Title：<%= book.title %></p>
  <p>Opinion：<%= book.opinion %></p>
  <p>ユーザーネーム：<%= book.user.name %></p>
</div>
<% end %>